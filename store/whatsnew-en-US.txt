SatStack 1.0.3

JSON backups are encrypted with the password in Security Settings (not the app PIN). After reinstall, enter that password, then import the JSON file. CSV and PDF stay plaintext.

Chart: purchase marks, extra time ranges (2Y–10Y), and clearer axis labels.

App PIN and JSON password are separate in Security Settings.
